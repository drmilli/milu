[build]
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "next start -p ${PORT:-3003}"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
